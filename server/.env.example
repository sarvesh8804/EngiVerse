# Example environment variables for server
# GitHub token for server-side proxy (recommended: create a PAT with repo scope as needed)
GITHUB_TOKEN=ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Stripe secret is already in `server/index.js` but you should set your own in production.
STRIPE_SECRET=
